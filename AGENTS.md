# LEDPanelRescue Local Agent Rules

1. Never guess paths.
2. Before any patch/build/relaunch work, verify the live app binary path and running process path from the system state.
3. Before reporting a fix as deployed, verify the deployed bundle contains the expected strings/behavior markers.

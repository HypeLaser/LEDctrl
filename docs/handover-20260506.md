# LEDctrl Session Handover
**Date:** 6 May 2026, ~02:55 BST
**Status:** Fitted mode WORKING. Marquee mode PARTIAL (gaps inconsistent).

---

## What Works ✅

### Fitted Mode
- Displays all rows simultaneously on each page
- Paginates through all content correctly (no truncation)
- Uses 'b' mode (auto-typeset ON) + plain `0x0d` separators + Jump Out effects
- Fixed `formatText()` to preserve explicit newlines and removed `.prefix(7)` truncation
- Hold time always 4 digits (`%04d`) regardless of mode

### Marquee Mode (Partial)
- Continuous scroll works
- Text body matches Editor v3.99 capture byte-for-byte
- Uses 'a' mode (auto-typeset OFF) + plain `0x0d` separators
- Effects matched to Editor: Random (`0x2f`) In/Out, align `0x31`

---

## What's Broken 🔴

### Marquee Row Gaps
Gaps between rows are inconsistent:
- Row 1 → 2: ~100%
- Row 2 → 3: ~70%
- Row 3 → 4: ~70%
- Row 4 → 1 (cycle): ~200%

**Root cause hypothesis:** Our NMG payload matches the Editor capture. The difference must be in the **sequence file** or **control packet timing** that we haven't compared yet.

---

## Key Code Changes Made

1. **`SigmaClient.swift` — `makeNmg()`**
   - 'b' mode for Fitted, 'a' mode for Marquee, 'b' only for time tokens
   - Always 4-digit hold time
   - Removed NGP/Editor NMG path for simple sends

2. **`SigmaClient.swift` — `formatText()`**
   - Preserves explicit newlines regardless of mode
   - Removed `.prefix(7)` truncation
   - Single-line Marquee text collapses to one line

3. **`SigmaClient.swift` — `renderMultiRowBytes()`**
   - Split into `renderFittedBytes()` and `renderMarqueeBytes()`
   - Both use plain `0x0d` separators (matches Editor capture)

4. **`RescueModel.swift` — `optionsForCanvas()`**
   - Fitted: forces Jump Out (`0x30`) In/Out
   - Marquee: forces Random (`0x2f`) In/Out, align `0x31`

---

## Next Steps

1. **Capture full Editor packet stream** for Marquee multi-row and compare every byte:
   - NMG payload (✓ already matches)
   - Sequence file (`SEQUENT.SYS`)
   - Control packets (`04:01`, `04:02`)
   - Timing between packets

2. **Focus on sequence file differences:**
   - The `26 20 05 20` timing blocks may control scroll speed/gaps
   - Editor may send a different sequence file for Marquee vs Fitted

3. **Verify sequence file format:**
   - Current format: `53 51 04 00 01 00 00 00 44 54 0f 7f 26 20 05 20 ...`
   - Compare byte-for-byte with Editor's sequence file

---

## Files Modified This Session

- `Sources/SigmaProtocol/SigmaClient.swift` — mode logic, formatText, renderers
- `Sources/LEDctrl/ViewModels/RescueModel.swift` — optionsForCanvas effect overrides
- `docs/protocol-reference.md` — updated with all findings

## Deployed Binary

- `/Applications/LEDctrl.app` (PID 47949 at end of session)
- Build: `02:53:51` (final Marquee effect fix), then redeployed at `02:55` with docs
